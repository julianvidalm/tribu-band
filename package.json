{
  "name": "setlist-la-tribu",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/check.js",
    "test": "node --test \"test/**/*.test.js\"",
    "build": "node scripts/check.js && node build/build.js"
  }
}
