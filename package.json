{
  "name": "setlist-la-tribu",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/check.js",
    "test": "node --test \"test/**/*.test.js\"",
    "build": "node scripts/check.js && node build/build.js",
    "build:show": "node scripts/check.js && node build/build.js --show",
    "build:songs": "node scripts/check.js && node build/build.js --songs",
    "deploy": "bash scripts/deploy-pages.sh"
  }
}
